<context path=".opencode" updated="2026-09-23">

This directory configures local OpenCode additions. It contains one plugin
loader, four native skills, and four matching slash commands.

## Summary

- .gitignore — excludes local package files, lockfiles, dependencies, and itself.
- bun.lock — Bun dependency lockfile for the local plugin package.
- commands/ — slash commands: agent-browser, grill, humanizer, and web-search.
- node_modules/ — locally installed plugin dependencies.
- package-lock.json — npm dependency lockfile for the local plugin package.
- package.json — local plugin package metadata and runtime dependencies.
- plugins/ — exec-wrapper-guard.ts re-exports the source plugin for auto-loading.
- skills/ — native skill directories: agent-browser, grill, humanizer, web-search.
- tests/ — currently empty.
- tsconfig.json — type-checks the plugin loader.

## Constraints

- `plugins/exec-wrapper-guard.ts` MUST re-export the source implementation.

## Guidance

- Keep each command filename aligned with its same-named skill directory.

</context>
