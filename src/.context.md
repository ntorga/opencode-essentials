<context path="src" updated="2026-09-23">

One OpenCode plugin suite with three product entry points and one local guard.
The server entry runs the features; the TUI companion lets the user switch
everything off, toggle each feature, and tune its idle timeout at runtime; the
TUI idle clock renders the wait for input on screen. OpenCode loads the local
guard through `.opencode/plugins/exec-wrapper-guard.ts`.

## Summary

- server.ts — server entry. Default export `{ id, server }`. Builds each
  feature's hooks from per-feature options and fans them out via
  `combineHooks`.
- server.test.ts — checks the server entry, option routing, unknown features,
  and state-file toggles.
- tui.ts — TUI entry. Default export `{ id, tui }`. Registers the palette
  command `essentials.features` (`/essentials`), which opens a
  `DialogSelect` with the master switch, one row per feature, one row per
  adjustable timeout, and one row per adjustable token ceiling.
- tui.test.ts — checks the palette command, feature toggles, timeout and
  ceiling dialogs, validation, and config errors.
- idle-clock.tsx — second TUI entry. Default export `{ id, tui }`. Registers
  an `app_bottom` slot that shows the idle duration and local idle-start date
  and time. It reads state each tick and renders only when config is readable,
  the clock is enabled, and the session is idle with a completed-response
  anchor. It caps the effective timeout with the shared helper in
  `valueObject/idleTimeoutMs.ts`.
  JSX and `solid-js` resolve to the host's runtime modules.
- contextCeiling.ts — the ceiling compactor's pure logic: measure the newest
  real completed turn, read the model's context window from the provider
  list, and clamp the chosen ceiling to it.
- contextCeiling.test.ts — checks turn measurement, provider-window lookup,
  and model-window clamping.
- idleWaiting.ts — the clock's pure logic: read host Message shapes, pick the
  newest completed assistant response other than a compaction summary as the
  idle anchor, format the elapsed wait and local start time, and color the
  line by the enabled idle compactor's timeout: yellow at 50%, red at 80%.
- idleWaiting.test.ts — checks message conversion, idle anchors, local time
  display, and timeout-based colors.
- state.ts — shared config protocol. A versioned JSON file at
  `$XDG_DATA_HOME/opencode/essentials.json`. The TUI writes it; the server
  reads it at decision points. A pre-version flat boolean map migrates on
  the first write.
- state.test.ts — checks config defaults, migration, settings persistence,
  and malformed or unsafe state files.
- requestDeadline.ts — the shared 60-second deadline for requests to the
  local OpenCode server.
- valueObject/ — the trust boundary. Branded types whose `New*`
  constructors are the only admission path for external strings and numbers.
- hooks.ts — `combineHooks`. Fans one hook out to all features.
- hooks.test.ts — checks ordered fan-out for event, message, and dispose
  handlers.
- log.ts — replaces control characters with spaces before it writes structured
  logs through `client.app.log`.
- log.test.ts — checks that Unicode control characters become spaces in logs.
- exec-wrapper-guard.ts — checks wrapped shell commands against specific bash
  rules from agent profiles. It uses the Bash parser and fails open when
  evaluation fails.
- exec-wrapper-guard.test.ts — checks wrapper parsing, rule decisions, and
  evaluation failures.
- features/ — the feature suite. Each feature is a `SuiteFeature` entry.
- README.md — installation, configuration, and exact semantics.

## Constraints

- Product entry modules MUST stay free of installed runtime dependencies.
  They import types and Node built-ins. The clock imports `solid-js` and
  `@opentui/solid`, which the OpenCode TUI host provides as runtime modules.
  The local guard uses parser packages from devDependencies.
- Product entry modules MUST export either `server()` or `tui()`, never both.
  The local guard exports its own OpenCode plugin function.
- MUST register the server entry in `opencode.json` and both TUI entries in
  `tui.json`. The two hosts read separate config files; the TUI host never
  loads `opencode.json`.
- MUST NOT require a restart to change a feature. The server reads the state
  file at each decision point.
- MUST NOT overwrite a state file that cannot be read. A corrupt file must
  fail loud, not silently reset stored choices.
- MUST NOT poll. The compactor is event-driven with one-shot timers.
- MUST route every external string or number through a constructor in
  `valueObject/` before it reaches business logic.

## Guidance

- Add a new feature as a `SuiteFeature` in `features/` and register it in
  `features/registry.ts`. Both entries pick it up automatically.
- `combineHooks` runs feature handlers sequentially without isolation: one
  feature throwing aborts the rest for that event. Wrap the handler when a
  second feature lands.

</context>
